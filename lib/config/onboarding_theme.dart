import 'package:flutter/material.dart';

/// Brand colors for onboarding screens (from design board).
abstract final class OnboardingTheme {
  static const orange = Color(0xFFFF8C00);
  static const teal = Color(0xFF20B2AA);
  static const navy = Color(0xFF1A3B66);
  static const grey = Color(0xFF607D8B);
}
